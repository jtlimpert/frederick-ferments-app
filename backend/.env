# backend/.env - Environment variables for development

# Database connection for local Docker development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/frederick_ferments

# For when running API in Docker (container-to-container communication)
# DATABASE_URL=postgresql://postgres:postgres@db:5432/frederick_ferments

# Server configuration
PORT=4000
RUST_LOG=info

# Add other environment variables as needed
# JWT_SECRET=your-secret-key
# CORS_ORIGIN=http://localhost:3000